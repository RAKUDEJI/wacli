// Generated by `wit-bindgen` 0.51.0.

///|
pub fn list_commands() -> Array[@types.CommandMeta] {
  [@greeter.meta()]
}

///|
pub fn run(name : String, argv : Array[String]) -> Result[UInt, @types.CommandError] {
  let meta = @greeter.meta()

  // Check if name matches command name or aliases
  if name == meta.name {
    return @greeter.run(argv)
  }

  for i = 0; i < meta.aliases.length(); i = i + 1 {
    if name == meta.aliases[i] {
      return @greeter.run(argv)
    }
  }

  Err(@types.CommandError::UnknownCommand(name))
}

