// Generated by `wit-bindgen` 0.51.0.

///|
pub fn meta() -> @types.CommandMeta {
  {
    name: "greet",
    summary: "Greet someone",
    usage: "greet [name]",
    aliases: ["hello", "hi"],
    version: "0.1.0",
    hidden: false,
  }
}

///|
pub fn run(argv : Array[String]) -> Result[UInt, @types.CommandError] {
  let name = if argv.length() > 0 { argv[0] } else { "World" }
  let message = "Hello, \{name}!\n"
  let bytes = @encoding/utf8.encode(message)
  let fixed : FixedArray[Byte] = FixedArray::makei(bytes.length(), fn(i) { bytes[i] })
  @host.stdout_write(fixed)
  Ok(0)
}

